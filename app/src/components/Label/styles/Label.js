import styled from 'styled-components';

export const Wrapper = styled.span`
    padding: 0.9rem;
    font-size: 1.3rem;
    border-radius: 8px;
    color: rgb(67, 97, 238);
    background: rgba(067, 97, 238, 0.1);
    height: fit-content;
`;